singularity {
  enabled = true
  autoMounts = true
  runOptions = '-B /hpc/:/hpc/ -B $TMPDIR:$TMPDIR' }

process {
    executor = 'slurm'

    withLabel: 'long' {
        cpus = 8
        memory = 35.GB
        time = 1.h
    }

    withLabel: 'short' {
        cpus = 4
        time = 1.h
    }
}
